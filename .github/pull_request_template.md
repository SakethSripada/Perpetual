## What this changes

<!-- What behavior is different, and why. Link an issue if there is one. -->

## How it was verified

<!-- Tests added, and how you exercised the change in the running extension. -->

## Checklist

- [ ] `npm test` passes
- [ ] `cargo test --workspace`, `cargo fmt --all -- --check`, and `cargo clippy --workspace --all-targets -- -D warnings` pass if Rust changed
- [ ] Behavior changes are covered by a test
- [ ] Packaging notes included if this affects the daemon or a platform target
