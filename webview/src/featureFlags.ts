// Kept in one tiny module so re-enabling these preserved implementations does
// not require rediscovering scattered UI conditions.
export const CLOUD_CONTINUITY_ENABLED = false;
export const LAN_COLLABORATION_ENABLED = false;
